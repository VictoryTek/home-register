# ============================================================================
# Home Registry Environment Configuration
# ============================================================================
# Copy this file to `.env` and customize values for your deployment.
# The `.env` file is excluded from Git for security.
#
# SECURITY NOTICE:
# - Generate strong passwords using a password manager
# - Use 16+ character passwords for production deployments
# - Never commit your `.env` file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration (Required for custom password)
# ----------------------------------------------------------------------------

# PostgreSQL database password
# ⚠️  IMPORTANT: Set your own secure password here!
#
# The docker-compose.yml file contains a randomly generated default password,
# but you should ALWAYS set your own password here for any real use.
#
# Generate a secure password:
#   openssl rand -base64 24
#   # Or use a password manager
#
# Requirements:
#   - Minimum 16 characters (20+ recommended)
#   - Mix: uppercase, lowercase, numbers, symbols
#   - Avoid: @ : / characters (conflict with connection strings)
#   - Examples:
#     - Generated: 7mK$9pQx2#nLwR5tY8vB3zF
#     - Passphrase: correct-horse-battery-staple-2026
#
# IMPORTANT: If you change this after initial setup, you must recreate the database:
#   docker compose down -v && docker compose up -d
#
POSTGRES_PASSWORD=your_secure_random_password_here

# ----------------------------------------------------------------------------
# Application Configuration (Optional - defaults provided)
# ----------------------------------------------------------------------------

# HTTP server port (default: 8210)
# PORT=8210

# Logging level: error, warn, info, debug, trace (default: info)
# RUST_LOG=info

# JWT secret for authentication token signing (auto-generated if not set)
# - If not set, a random secret is generated and persisted to /app/data/jwt_secret
# - For production: set explicitly to ensure consistency across container recreations
# - Generate with: openssl rand -base64 32
# - Example: JWT_SECRET=abcdef1234567890ABCDEF1234567890
# JWT_SECRET=

# JWT token lifetime in hours (default: 24)
# JWT_TOKEN_LIFETIME_HOURS=24

# API rate limiting - requests per second (default: 100)
# - Protects server from being overwhelmed by aggressive API clients
# - Adjust based on your expected traffic and server capacity
# RATE_LIMIT_RPS=100

# API rate limiting - burst capacity (default: 200)
# - Allows temporary spikes above RPS limit
# - Handles page load spikes with multiple simultaneous requests
# RATE_LIMIT_BURST=200

# ----------------------------------------------------------------------------
# Advanced Configuration (Production Only)
# ----------------------------------------------------------------------------

# PostgreSQL version (default: 17)
# POSTGRES_VERSION=17

# Container image version for production deployments (default: beta)
# VERSION=beta

# GitHub repository owner for production image (default: victorytek)
# GITHUB_REPOSITORY_OWNER=victorytek
